{
  "name": "liar-game-helper",
  "version": "1.0.0",
  "description": "Liar Game helper site for voice chats",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "server": "nodemon index.js",
    "client-install": "npm install --prefix client",
    "client-build": "npm run build --prefix client",
    "client-dev": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client-dev\"",
    "build": "echo 'Build skipped: assets are pre-built in public directory'",
    "install-all": "npm install && npm run client-install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "basic-ftp": "^6.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "nodemailer": "^9.0.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "nodemon": "^3.1.9"
  }
}
